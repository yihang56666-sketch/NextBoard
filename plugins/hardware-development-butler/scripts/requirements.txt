# Minimal runtime dependencies for the source workspace.
#
# Optional integrations live in pyproject extras:
#   pip install -e ".[ui]"
#   pip install -e ".[hardware]"
#   pip install -e ".[ai]"
#   pip install -e ".[all]"

pyserial>=3.5
